{
  "name": "@neip/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "nEIP MCP Server — Model Context Protocol for AI integration",
  "main": "./dist/index.js",
  "bin": {
    "neip-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist .turbo tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "25.5.0",
    "typescript": "*"
  }
}
